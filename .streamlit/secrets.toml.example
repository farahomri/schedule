# Copy this file to secrets.toml and fill in real values.
# secrets.toml is excluded from version control (.gitignore).
# On Streamlit Community Cloud, set these as app secrets in the dashboard.
# On other environments, set as environment variables with the same key names.

ADMIN_PASSWORD = "change-me"
MANAGER_PASSWORD = "change-me"
USER_PASSWORD = "change-me"
